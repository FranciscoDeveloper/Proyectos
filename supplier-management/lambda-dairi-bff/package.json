{
  "name": "dairi-lambda-entities",
  "version": "1.0.0",
  "description": "Lambda handler for CRUD /api/entities/{entity} — suppliers, products, expenses against RDS PostgreSQL",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "build": "npm install --omit=dev",
    "zip": "zip -r ../lambda-entities.zip index.mjs package.json node_modules/"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/s3-request-presigner": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.3"
  }
}
