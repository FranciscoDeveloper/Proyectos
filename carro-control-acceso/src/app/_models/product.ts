export class Product {
    quantity: string;
    id: string;
    price: string;
   constructor( 
    id: string,
    price: string,
    quantity: string){
     this.quantity=quantity;
     this.id=id;
     this.price=price;
    }
 
}

